<Page x:Class="BLA.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BLA"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1200"
      Title="AboutPage">

    <Page.Resources>
        <!-- Стиль для кнопок -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="#1C1C1C" >
        <Border Background="#222222" CornerRadius="10" Padding="10" BorderBrush="#333333" BorderThickness="1">
            <Grid>
                <!-- Информация о приложении -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Height="508" Width="1104"  >
                    <TextBlock Text="О приложении" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Версия: 0.2.1" Foreground="White" FontSize="20" HorizontalAlignment="Center" />
                    <TextBlock Text="Разработано: Крюков Максим inc." Foreground="White" FontSize="20" HorizontalAlignment="Center" Margin="0 0 0 20"/>
                    <TextBlock Text="Описание приложения: Данное приложение предназначено для учёта запчастей, используемых в беспилотных летательных аппаратах (БПЛА). Приложение предоставляет возможность добавления, изменения и удаления записей о запчастях, а также управления данными партнёров. Пользователи могут легко просматривать карточки запчастей, которые содержат подробные характеристики, а также фотографии каждой детали. Интуитивно понятный интерфейс приложения позволяет быстро и эффективно управлять всеми записями, обеспечивая точность учёта и лёгкость в навигации. Приложение поддерживает функционал добавления и удаления партнёров, что делает его удобным для ведения учёта поставок и взаимодействия с внешними контрагентами. Возможности изменения темы и языка интерфейса создают комфортные условия для работы в любых условиях." Foreground="White" FontSize="20" TextWrapping="Wrap" MaxWidth="800" Height="389" Width="1044" />
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Page>
